enum Resource {
  ORGANIZATION,
  USER,
  PROJECT,
}

enum UserAction {
  REGISTER,
  LOGIN,
  LOGOUT,
  GET_PROFILE,
}

enum OrganizationAction {
  GET_ONE,
  GET_ALL,
  CREATE,
  UPDATE,
  DELETE,
}

enum PROJECT {
  GET_ONE,
  GET_ALL,
  CREATE,
  UPDATE,
  DELETE,
}
